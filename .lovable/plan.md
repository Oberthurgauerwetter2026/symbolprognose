Gesamthöhe des Zeitslider-Cards in `src/components/region-map.tsx` reduzieren:

- Card-Padding: `p-5` → `p-3`.
- Header-Abstand zum Slider: `mb-4` → `mb-2`.
- Stundenlegende-Block: `mt-3` → `mt-2`, Tick-Höhe `h-2` → `h-1.5`, Label-Block `mt-1 h-4` → `mt-0.5 h-3`, Label-Font `text-[11px]` → `text-[10px]`.
- Footer "jetzt / +Tage": `mt-3` → `mt-1.5`.

Slider-Track/Thumb (CSS) bleibt wie aktuell (4 px / 16 px).